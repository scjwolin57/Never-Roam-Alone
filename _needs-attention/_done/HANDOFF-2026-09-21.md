# Handoff, 2026-09-21

*Read CLAUDE.md, then `decisions.md` (newest rows first), then this. Everything below is
committed and pushed unless it says otherwise. Main folder = GitHub at the commit named in
the last line of this file's git log.*

## Finished this week (all live)
- **One daily-cost formula**: `cost-estimator.js` feeds the city guide, the finder and the
  directory sort. No static daily figure anywhere. cities.html reads `destinations.js`.
- **Hotel card** rebuilt: avg per night + info bubble, budget/mid/high row, Laundry, Gym,
  and the **Find laundry & gyms** modal (neighborhood dropdown; plain Google Maps links, no
  map, no page key).
- **Neighborhood coordinates** (`hood_geo`): 3,878 of 4,053 placed. 175 unplaced are in
  `hood-geo-unresolved.md`, waiting for Jeff's pins.
- **Gyms**: 893/893 researched, 2,656 gyms, 92 cities honestly empty. Sheet tab "Gyms".
- **Laundromats per neighborhood**: Overture open data, 1,964 listings, 474 cities. Sheet
  tab "Laundromats". Empty = "None listed ... in our open-data source".
- **Day trips verified**: 3,806 → 2,670. Rules in `RESUME_daytrip-verify.md` (12 checks):
  never another guide city, boats on the clock, no flight *to get there* (a scenic flight as
  the activity is fine), GetYourGuide search deep link on every kept entry. Sheet synced
  893/893. Cell format: `<Name> (<Country>) — <blurb>`, em dash, `★ ` before featured.
- **Rain**: average rainy days per month per quarter, NASA POWER, all 893. Overflow fixes
  from the phone scan merged. The `overflow-fixes` branch is fully in main.
- **CLAUDE.md** is the approved rulebook; `~/.claude/CLAUDE.md` holds the universal parts.
  Rule added: a change Jeff initiates that crosses the rulebook is flagged for his review.

## Open, in the order I would do them
1. **Nearby guides section (not built).** 609 pairs in each city's `nearby` key, 355 cities;
   nothing reads it and the change protocol (CLAUDE.md §5.3) was never run for it. The
   tiers are unverified: 77 "half" pairs are over 100 km, 24 "full" over 250 km, 317 pairs
   are one-directional, 31 cities have nearby links but no day trips (the day-trip section
   is hidden there). **Proposed to Jeff, awaiting his yes:** show distance instead of tier,
   mirror the links both ways, limit 150 km, four nearest per city, own heading, site-only
   and regenerated by add_city.py.
2. **66 cities with no day trips.** Research candidates from GetYourGuide's own city pages
   (no API: it needs 100,000 monthly visits), verify each against rules 1-9, never pad.
   List is in `RESUME_daytrip-verify.md`.
3. **Eat / coffee / drink per neighborhood.** Plan in `hood-picks-plan.md`, nothing
   collected. Eight decisions D1-D8 wait on Jeff; D1 is Google Places at about $390. The
   Google trial credit has a deadline (decisions.md 2026-09-18), so this is time-sensitive.
4. **Jeff's calls from the day-trip pass:** Port-au-Prince/Jacmel and Erbil/Amadiya left
   unverified; Chiang Rai's Mae Salong is both a neighborhood and a day trip; possible
   padding in old data for Montevideo, Tel Aviv, Dushanbe.
5. **129 cities whose neighborhood list differs sheet vs site** (`photo-sheet-sync-questions.md`),
   and the day-trip slots still sitting in neighborhood lists (`hoods-that-are-day-trips.md`).
6. Older backlog, unchanged: 2,942 landmark coordinates unchecked, 562 landmarks without a
   photo, 427 Flickr requests unsent, hood-photo judgement calls (README.md has the counts).

## Housekeeping (done 2026-09-21)
- Launch config reverted to the committed version (only the `static-site` preview).
- Merged branches deleted, locally and on GitHub: `overflow-fixes`, `claude/vibrant-gates-238a2a`,
  `rain-overflow-reapply`; their worktrees removed. Main is the only branch.
- The day-trip pass's per-batch verdict records (batches 2-36, 323 files, the only record of
  each entry's sources) were copied into the main folder's `_guidebuild/daytrips/batches/`
  first, with the newer `DAYTRIP_BRIEF.md` (flight rule updated to the amended wording).
- The scheduled task `nra-daytrip-verify` is disabled. One last run fired at 13:00 local just
  as it was being disabled; it was stopped and changed nothing.
- Two detached preview worktrees from another session remain in a scratchpad under /private/tmp
  (`laundry-preview`, `pilot-preview`). They carry no branch; harmless.
- `GOOGLE_MAPS_API_KEY` is in `.env`. No Google key is in any page. Never print it.

## Traps that cost time this week
- **City files are one line each.** Two branches that both touch citydata cannot be merged
  by git: about 890 conflicts. Merge by key instead: keep main's file and write the other
  side's changed keys in by character span (see commit e5c0b674 for the method). Land one
  data job at a time.
- **`_guidebuild/` is gitignored** and lives only in the main folder. A session in a
  worktree has no build scripts unless it copies them. `checkjs.py` is there too.
- **Do not run `extend_citydata.py`**: its catalogs are stale and it wipes newer data.
- The web-search budget is about 200 per session, shared by every agent in it.
- openpyxl: `ws.cell(row, col, value=None)` does not clear a cell. Assign `cell.value`.
- The main folder can fall behind GitHub when work lands from a worktree. `git fetch` and
  compare before any site-wide job.

## RETIRED 2026-09-23
Superseded: every open item is tracked in its own file (hood picks: RESUME_hood-picks.md; day-trip calls: daytrips-open-questions.md; photo-sheet-sync-questions.md; hoods-that-are-day-trips.md; RESUME_landmark-pin-pass.md).
