# Decisions log

*One line per decision, newest first: date, what was decided, why. Every session
reads this before proposing or re-researching anything. Rules adopted in chat go
here the same day. See CLAUDE.md §4.5 (this file's companion rulebook).*

| Date | Decision | Why / source |
|---|---|---|
| 2026-09-22 | **Hood picks gap pass on batch 1 (items 1, 3, 4).** Empty slots were 385 of 1,190 after loading, and the causes were mechanical: one 20-result Nearby Search per hood and section; kinds typed from Google's tags (110 slots had venues in the hood but none tagged for the kind); four hoods never searched for lack of a point. Adopted: (1) the pick agent classifies the kind from the venue's own page across the whole hood pool (`kind_by: "page"`, accepted by check_picks; the independent checker confirms the kind); (3) one Text Search per empty slot (`gap_fill.py search`, 359 calls, $7.72 of the trial credit); (4) Shinjuku, Patong and Muratpaşa points placed from OpenStreetMap (place nodes, recorded in the commit), Belek left unplaced: it is 35 km out and really a day trip. Not adopted (not asked): paginating Nearby to 60 results; adjacent-district and hotel-venue relaxations. | Jeff: "yes, do 1, 3 and 4 on batch 1" after the cause breakdown (51% agent-rejected, 29% untyped, 8% unsearched hood, 7% no result, 5% neighbor-only). |
| 2026-09-22 | **Batch 1 loads only after one more kind pass (option B).** Every `party`, `takeaway`, `bakery` and `coffee` pick gets a fresh kind check under tightened definitions (party = a place people go to dance or drink late with music, not a cocktail bar; takeaway = you order at a counter and can leave with the food; bakery = baked goods or pastry are the main product; coffee = coffee is the main product), then a fourth fresh 10% audit, then sheet, citydata, commit. | Jeff: "B in hood-picks-batch1-load.md". |
| 2026-09-22 | **Travel advisories go multi-source, keyed to the reader's passport.** A `passport_country` on the profile (private to the account, never shown on a public profile); a signed-in traveller sees their own government's advisory when it is tracked, everyone else sees three: **US, UK, Canada**. A banner shows at **"avoid non-essential travel" and above** and nothing below it. Advisories move out of citydata into one country-level file with per-city regional overrides, each source carrying its own label, link and publication date (the hardcoded "as of Jul 2026" goes). The internal rule stops naming one government: a place is restricted when any tracked government advises against all travel there. Spec and stages: `advisory-source-scope.md`. | Jeff: "my plan is to update the profile to have a passport country attached to each... For non signed in users it can display the 3 government", then "us, uk, canada. banner at avoid non-essential and above. keep it private". |
| 2026-09-22 | **Landmark pins, Google pass moved to 2 October 09:00** (scheduled task `landmark-pins-google-pass`). Running it in September would cost about $10 of the $281 trial credit (2,056 geocoding lookups at $5/1,000; September's free 10,000 were already spent); October's free 10,000 cover it at no cost. The run now starts with two gates: confirm October's free geocoding allowance has reset (else stop, no paid calls, no trial credit), and confirm no hood-picks load is writing citydata or the sheet. | Jeff: "wait until oct 2, set an automated reminder to check the google free lookups and (if the reset is enabled) complete the task on Oct 2". |
| 2026-09-22 | **Punta Cana's Manatí Park removed: confirmed closed for renovations.** A closure is a deletion with the source in the log, so the entry is out of the three catalogs, citydata and the sheet; Punta Cana ships with 9 landmarks and is not padded. Details for restoring it if it reopens are in `manati-park-closed.md`. The same pass fixed the sheet's Tozeur row, which still listed the El Ferdous Mosque that the September merge removed from the site; sheet and catalogs now agree on landmark names for all 893 cities. | Jeff: "Punta Cana's Manati Park is confirmed closed for renovations"; OSM labels the site Cerrado. |
| 2026-09-22 | **Quito's El Panecillo pin restored to the hill.** The 16 September sweep (`3a04f7d2`) had moved it 4.7 km north; it is now -0.2286111, -78.5186111, the point Wikidata gives for both the hill and the Virgin of El Panecillo. This is the first protected slot this pass overwrote, and only because Jeff decided it; `apply_pins.py` still refuses protected slots unless `--allow-protected` is passed. | Jeff: "you can use the Virgin of El Panecillo coordinates if that helps, same place". |
| 2026-09-22 | **A landmark change is not live until citydata has it.** city.html reads `citydata/<slug>.json`; the catalogs (`city-landmarks.js`, `city-landmark-coords.js`, `city-landmark-photos.js`) are canonical but are only the source. Three September commits updated the catalogs alone, so 92 coordinate fixes in 73 cities (Aomori from Jeff's own pin, Avignon 27 km, Beihai 60 km), ten cities' photo lists and the Tozeur mosque merge never reached the site. Mirrored in commit b906db8d. From now on every catalog edit updates citydata in the same commit, and the check is "catalogs and citydata agree for all 893 cities", not "node --check passes". | Found during the landmark pin pass. |
| 2026-09-22 | **Landmark swaps from the empty-city day-trip calls.** St. George's: Annandale Falls replaces York House and Parliament Building. Ashgabat: Nisa replaces the Ashgabat Flagpole. El Calafate: Perito Moreno Glacier and Los Glaciares National Park leave the landmarks and become day trips, as one entry "Perito Moreno Glacier (Los Glaciares National Park)" because every El Calafate trip is inside the park (a separate park entry would list the same place twice). With Estancia Cristina and Upsala also gone, El Calafate keeps 6 landmarks; not padded. Applied with the day trips once the hood-picks run has finished. | Jeff: "annadale falls can replace york house, replace flagpole, move the two to day trips". |
| 2026-09-22 | **Postcards, archival prints and satellite images are never landmark photos.** A landmark photo shows the place as a visitor sees it today: no postcards (tinted or captioned), no pre-1960 archival prints, no satellite or space-station images, no carving close-ups, no watermarks. Each one found is replaced with a modern Commons photo whose location is proven by the landmark's own Commons category or GPS; if none exists the slot is cleared to "photo coming soon". First sweep: 83 suspects reviewed, 25 replaced, 12 cleared, 46 kept (paintings that are themselves the landmark, like the Ghent Altarpiece, stay). | Jeff: Miami's Lummus Park "photo is a postcard"; then "go". |
| 2026-09-22 | **Day-trip calls for the empty cities (13 calls in `daytrip-empty-cities-calls.md`), decided.** Kept as day trips: Debre Bizen from Asmara (full; the blurb says men-only and names the 1.5-3 h hike), Otse from Gaborone (half; the blurb says visits need a wildlife-department permit), Kpatawee (Monrovia), Savai'i (Apia), Ilhéu do Rei (Bissau), Lagoa Azul (São Tomé, no clash with the Óbô landmark), Saltinho (Bissau), Borith Lake (Karimabad), Lésio-Louna (Brazzaville). Annandale Falls (St. George's) and Nisa (Ashgabat) become landmarks, not day trips (both cities already have 10: which one each replaces is open). El Calafate's Estancia Cristina and Upsala boat trip move from landmarks to day trips. Majuro: Arno comes out of the "Outer Islets" hood name and becomes a full-day trip. Carriacou stays out until the new ferry timetable is confirmed; Brades is re-looked at if the Antigua ferry restarts. Nothing is applied until the hood-picks run has finished with citydata and the sheet. | Jeff: "1. keep (with a note about men only and the hike) 6a. keep (with a note about a permit needed). Yes to the rest". |
| 2026-09-22 | **Every neighborhood point is checked for location before any hood-picks batch spends Google calls, all 893 cities.** Batch 1 found 10 wrong points in 120, and each wrong point wasted its Google calls and every pick made around it. The check is `_guidebuild/hoodpicks/hoodpoint_check.py` (Photon / OpenStreetMap reverse and forward geocode; Nominatim rate-limits a full scan), suspects are reviewed with `hoodpoint_review.py` (landmark and OSM agree = auto-fix; otherwise Jeff decides), fixes go to citydata by span and the sheet's Hood N Latitude/Longitude in one commit. Full scan of the 3,868 placed points started 2026-09-22. | Jeff: "That should be completed before this work or we are just wasting time and money". |
| 2026-09-22 | **Every landmark pin is re-checked: OpenStreetMap now, Google after 1 October.** All 8,926 slots are looked up in OpenStreetMap; a pin changes only when OSM finds the same named place in the right city and Wikidata or a by-hand check agrees. The 93 slots changed by this month's coordinate commits (4 from Jeff's pins) are never overwritten; disagreements go to Jeff. Pins OSM cannot confirm wait for a Google pass on or after 2026-10-01, when the free geocoding quota resets (September is past it). Batches of 25 cities, committed per batch, not pushed. Log: `RESUME_landmark-pin-pass.md`. | Jeff: "go, use openstreetmap now and google after oct 1". A 120-pin sample found about 5% wrong, incl. Miami's Lummus Park. |
| 2026-09-22 | **Hood picks batch 1 re-picked under the new rules but still not loaded: the second fresh audit found 5 defects in 35 (14%).** 641 picks are staged (301 kept + 340 new, script-clean, independently checked, bars safety-searched), against 301 before; fill is 75% of slots including the baseline bars. Four of the five defects are one open question: whether a venue whose Google address names a sub-district of the hood's area (Si Phraya for Silom, Al Khatim for Quba, Bahçelievler for Konyaaltı) is "in the hood". The reviewers read the visitor's area as the hood; the auditor read the 2026-09-21 rule literally (address district must be the hood). Jeff decides in `hood-fit-definition.md`; the batch loads once an audit under the chosen reading is clean. Also found: 10 of the 120 batch-1 hood points were in the wrong place (fixed, `hood-points-batch1.md`); a Nominatim pre-flight (`hoodpoint_check.py`) now runs before every batch. Google calls this month: 845 of 1,000 free. | The re-pick run; RESUME_hood-picks.md batch log. |
| 2026-09-22 | **Hood picks: six rule changes after the batch-1 funnel review (25% of slots filled; Google and the script had candidates for 85-95% of eat and coffee slots, the agent rules dropped them).** (1) A Google listing that is OPERATIONAL, passes the type and quality checks and sits in the hood is proof the venue exists; the second source is for choosing between candidates and writing the note, never for elimination. No website = blank note, not a skip. (2) Chain detection is the script's job (same name at 2+ points in the city's own candidate cache, or an Overture brand tag); a venue with no such evidence is a single venue. Agents no longer count outlets. (3) Kinds loosened to what the page promises: `fine` = expensive price level or a guide listing (tasting menu not required); `takeaway` = any counter-service place eaten on the go (sandwich, slice, food court included); `bakery` includes pastry, churro and dessert shops; `local` = the city's own everyday traditional food, not folkloric only. (4) Rating bar: 4.2 first; a kind with no candidate at 4.2 in that hood drops to the city's median rating for the section (from its own candidates) and takes the top weighted-rating venue above it; the sheet row records which bar applied. Review bar for `dive` and `party` = 25 everywhere. (5) Bar safety is its own pass; a bar not yet safety-searched is held, never skipped or dropped. (6) Neighbor-hood pick: a kind still empty may take a spare qualifying venue from another hood within 1.5 km (that hood keeps its own pick), stored with the source hood and shown with a flag; a labelled exception to the 2026-09-21 hood-fit rule, not a reversal. Batch 1 is re-picked from the cache at zero Google cost; the 301 verified picks stay. | Jeff, on the funnel numbers: "the second source was supposed to be means to pick from multiple candidates (to pick the best one) not eliminate completely"; "review bar can be lowered as nightclubs barely reach 4.0... use the median as a base and then the top pick from there"; "empty slots are way too high". |
| 2026-09-21 | **Amsterdam's Canal Ring and Centrum hood points fixed.** Both had geocoded to a wrong point (a mismatched POI, and the city's own centroid). Re-queried by hand with a better search string (Grachtengordel for Canal Ring, the site's own hood_landmark "Dam Square" for Centrum); both are exact matches about 1 km from the city centre, and all 5 Amsterdam points are now 0.9-2.5 km apart with no overlap. Written into citydata/amsterdam.json. Amsterdam can now be included in a hood-picks batch. | hood-picks-amsterdam-hood-points.md, resolved. |
| 2026-09-21 | **Hood picks batch 1 not loaded: the audit failed (6 defects in 26).** Per the resume note one defect means the whole batch is re-checked before commit, and the session's search budget was gone. New rules in the brief: any brand with 2+ outlets is a local chain and the note must say so ("; local chain"); `local` means traditional food only; hood fit is by the address's district, an adjacent district is a skip. Bars need a scam / overcharging search from both the picker and the checker; 29 bars are held until it is run. Amsterdam is held for two misplaced hood points. Picks are staged on disk, not in the sheet or the site. | The batch-1 run; RESUME_hood-picks.md. |
| 2026-09-22 | **Antalya's Konyaaltı and Lara hood points stay on the beach strip; Mecca's Al Shubaikah point confirmed by Jeff's pin.** No changes. | Jeff. hood-points-batch1.md. |
| 2026-09-22 | **Google Places calls are Enterprise + Atmosphere, not plain Enterprise: $40/1,000, not $35.** `priceLevel` alone puts every call in that SKU (developers.google.com/maps/billing-and-pricing/pricing, checked 2026-09-22). Corrected `PRICE` in fetch_rank.py. Since the SKU was already the priciest one, added `editorialSummary`, `servesBeer/Wine/Cocktails`, `liveMusic`, `outdoorSeating`, `goodForGroups` to the field mask at no extra cost — not yet wired into kind classification (see the `dive` kind note in hood-fit-definition.md). Not retroactive to already-cached candidate responses. | Jeff: "can you use the google places api to extract more parameters". |
| 2026-09-22 | **Medina: Al-Anbariyah and Al Aqeeq removed as neighborhoods.** Medina now has 3 hoods (Quba, Qurban, Central Area (Al Haram)), not 5. Removed from every parallel citydata array (hoods, hood_tag, hood_desc, lodging, bars, hood_geo, hood_landmark) by span, and mirrored in the sheet (Hood 4/5 shifted into 2/3, Hood 4/5 cleared). city.html renders hood count dynamically, checkjs and check_schema_drift both clean. Neither hood had any hood-picks candidates that passed anyway. | Jeff: "remove medinah hoods" + a confirming pin for Mecca's Al Shubaikah (same message); clarified via AskUserQuestion. |
| 2026-09-22 | **Hood-fit definition resolved: reading A with the tightening.** A sub-district counts as inside a hood when it is genuinely part of the area a visitor means by that hood's name (e.g. Si Phraya is part of what "Silom" means); a sub-district that is itself one of the city's other tracked hoods is always out, even if nearby. Applied to the batch-1 second audit's 5 defects: kept The Shack, Leet, Al-Rowaithi Bakery; dropped Kans' Coffee (Antalya — its address names Muratpaşa, Antalya's own hood 3) and Frida (Milan — a separate `kind` defect: a cocktail bar picked as `dive`). | Jeff: "if the sub district belongs to the greater neighborhood it is allowed there". _needs-attention/hood-fit-definition.md. |
| 2026-09-21 | **Hood picks: how the three keys are stored.** `eat`, `cafes`, `bars` per hood are lists of `{k, n, d, pid}` (`d` and `pid` left out when absent; `pid` is the Google place ID and the map link uses it as `query_place_id`). The sheet's new "Hood Picks" tab (one row per pick, with source and article URLs) is the source of truth and `hoodpicks/load_picks.py` writes the keys into citydata by span; the 409 existing bars were seeded into the tab as baseline rows (never replaced). Order inside a hood is not data. Only place IDs, names, notes and URLs are kept, never Google's rating, review count or address. Per-month call counter and credit tracker (cap $260 of the $281, none after 2026-10-30) live in `fetch_rank.py`. The 1.2 km rule for whole-town and beach-strip hoods is not applied yet (the search radius is 800 m everywhere): decide per hood before the small-town batches. | Follows from the 2026-09-21 scope row; RESUME_hood-picks.md. |
| 2026-09-21 | **Hood picks gate 2 unlocked.** The free trial's billing account is linked to the never-roam-alone project, so after the free allowance the run uses the $281 trial credit (until it or the trial ends on 2026-10-30). Gate 3 (any out-of-pocket spend) still needs Jeff's explicit yes. | Jeff: "free trial is linked to never roam alone project". |
| 2026-09-21 | **Hood picks: stop on Google errors, one batch per session.** Any Google quota, billing or server error stops the run; nothing is cached or saved as blank, and the batch is redone when Google answers again. The run goes one batch per session (fresh web-search budget), with a scheduled task starting the next. The call counter is per month. Google's raw responses are deleted once a batch is committed. | Jeff (stop rule); the rest from the pre-run review in the same session. RESUME_hood-picks.md. |
| 2026-09-21 | **Where to eat / Coffee & takeaway / Where to drink: scope and method.** First phase = the 412 cities with 500,000+ international visitors (1,913 neighborhoods with a map point); all 10 kinds (3 eat, 3 coffee, 4 drink), one pick per kind; a one-line note for every pick written in the same pass, blank where no source supports one; an independent second check on every batch plus a 10% audit; runs autonomously in batches of 25. Candidates: one Google search per hood per section, then one per kind only where a kind is still empty (the pilot filled only 50% of slots with per-section searches). Definitions adopted from the plan's recommendations (quality bar 4.2+ and 100 reviews, 25 in towns; 800 m; no global chains; Latin-script names). Spec: `_needs-attention/RESUME_hood-picks.md`. | Jeff: "do notes as well now... Do the 10 slots... run the independent second check. Run autonomously continuous in batches". |
| 2026-09-21 | **Hood-picks budget gates.** Free Google allowance first; the $281 trial credit only after Jeff confirms the never-roam-alone project is linked to the billing account holding it (otherwise calls could bill a card); no out-of-pocket spend without his explicit yes. Estimated ~15,300 searches; free + credit by 2026-10-30 covers ~10,000; the rest is ~$190 or ~5-6 more months of free allowance. Rolling re-checks later on the free 5,000/month status lookups. | Cost discussion, this session and "Neighborhood guides research plan". |
| 2026-09-21 | **Nearby guides across water resolved.** Of the 36 pairs with no road route: 25 over 120 km of water are not day trips; of the 11 under 120 km, ferries were checked and 3 qualify (Philipsburg → The Valley by the Anguilla ferry, half day; Dar es Salaam ↔ Zanzibar City by Azam fast ferry, full day). The other 8 fail: no ferry, a weekly-only ferry, no same-day return (Makana, St Kitts–St Maarten), or a 12-hour crossing (Malabo–Douala). A ferry qualifies only if it runs often enough to go and return the same day. | Jeff: "trim the list and check the ferries". |
| 2026-09-21 | **Day-trip section headers read "Half day · 1.5 hours or less one way" and "Full day · 3 hours or less one way".** Classification stays at half about 75 min, full up to about 3 h (2026-09-18). The half-day header is deliberately looser than the rule, to allow for traffic: this is not a two-definitions bug, do not "fix" it. | Jeff. |
| 2026-09-21 | **Nearby guide cards.** Another Never Roam Alone guide within day-trip range is shown in the day-trip list it belongs to, after that list's day trips, as the existing day-trip card with a rust ribbon like the neighborhood photos' tags ("🧭 See Our Guide Page"); the whole card links to that guide. Placement is by travel time under the day-trip limits (half about 75 min, full up to about 3 h), from the day-trip pass's own recorded time where it noted one (95 pairs) or driving time from OSRM/OpenStreetMap (478): 199 half, 253 full, 121 over 3 h not shown, 36 with no road route listed in `nearby-guides-no-road-route.md`. The pass's stored tiers were not used: 157 of the shown pairs changed list (e.g. Mostar from Kotor was "half" at 231 min). Site-only; `nearby` key built by `_guidebuild/daytrips/build_nearby.py`. Also cleared 11 `featured` day trips whose "See guide" link went to a namesake in another country (Faro's Lagos → Lagos, Nigeria). | Jeff: "the current cards can be used for the nearby guides in the relevant section". |
| 2026-09-21 | **Laundromat lists loaded for all 893 cities** from Overture Maps open data under the 2026-09-18 definition: 1,964 listings, 474 cities and 1,102 of 3,868 placed neighborhoods have at least one; the rest are recorded as checked and empty. An empty neighborhood reads "None listed for X in our open-data source", not "coming soon", because it has been checked. Stored as `laundromats` per city and a Laundromats tab in the sheet. | Jeff: "run the laundromat load". |
| 2026-09-21 | **Rain = average rainy days per month in each quarter, computed from NASA POWER for all 893 cities.** A rainy day has 1.0 mm or more. Source: NASA POWER daily PRECTOTCORR (MERRA-2 reanalysis, public domain, free for commercial use) at each city's lat/lng, 2015-2024. `rain.qN` = rainy days in that quarter ÷ (years × 3), one decimal; the site, `city-seasons.js` and the sheet's Rainy Days Q1–Q4 all hold that per-month figure, and the pages no longer divide by 3. Computed by `_guidebuild/rain/recompute_rain.py`, never typed by hand; new cities use its `city` mode. Rejected: Open-Meteo (free tier is non-commercial only, and a 10-year request counts as hundreds of calls), NOAA stations (per-city station matching, gaps). Known limit: coarse grid, so dry desert cities read a little wetter (Cairo Q1 about 3/month vs 1 before). Replaces hand-typed values with no source, 253 of which were millimetres. | Jeff: "lets try nasa power (The figure should represent average number of rain days per month (separated by quarter)". |
| 2026-09-19 | **Religion and language rows: a catch-all is just "Other", never "Other (A, B, C)".** The list inside the brackets is dropped; the percentage stays. Applied to 12 rows, site and sheet together: religion in Agra, Dhaka, Hamadan, Mingora, Saint Martin's Island, Shiraz, Srimangal, Sylhet, Tehran ("Other (…)" → "Other"), Port-au-Prince ("None/Other (incl. Vodou practice)" → "None/Other"), Yerevan ("Non-religious / Other (incl. Yezidi)" → "Non-religious / Other"); language in Baku ("Other (Lezgin, Talysh, etc.)" → "Other"). Named groups that aren't a catch-all list, like "Other Christian" or "Protestant (various denominations)", are unchanged; long names now wrap on phones. New cities follow the same rule. | Jeff, on the phone-overflow scan: "just use 'other', no need to list them all". |
| 2026-09-19 | **Screen sizes: every page, popup and form must look right at 320, 360, 375, 390–430, 768 and 1280+.** 360 is the design baseline (most common Android width); 320 is the floor (zoomed text, fold cover screens, WCAG 2.1 reflow) and must look right too, not only avoid overflow. Every layout change is checked at all six widths. Replaces "desktop and the mobile preset" in CLAUDE.md §6. | Jeff: "check all the sizes listed above as a design rule. it should look right on all screens". |
| 2026-09-21 | **Amends the no-flights day-trip rule:** a flight is ruled out only as the way to *reach* the day trip. A flight that is part of the activity is fine (Maun's Okavango Delta keeps its scenic flight). | Jeff: "flight may be fine if it is part of the activity, not the way to get to the day trip location". |
| 2026-09-21 | **Day-trip pass landed on main after an error check.** The check found 12 guide cities still listed as day trips (Ziguinchor, Baden-Baden, Cinque Terre, Malmö, Cyangugu, Jerez de la Frontera, Positano, Cox's Bazar, El Chaltén, Gibraltar, Nîmes, Knysna); each was removed and added as a Nearby guide. 17 other name matches are namesakes and stay (Lagos in Portugal, Naples in Florida, Tripoli in Lebanon ...). Final: 2,670 day trips, 609 nearby links, sheet columns match day-trips.js for all 893 cities. | Pre-push verification, this session. |
| 2026-09-21 | **Amends the GetYourGuide row below: link first, verify by sampling.** GetYourGuide's partner API needs 100,000 monthly visits (Basic) or 1M visits plus 300 bookings a month (Reading), so it is out of reach before launch; the affiliate program's deep links and widgets have no traffic minimum. Every kept day trip gets a GetYourGuide search deep link built from its name, with no per-entry lookup. The "not listed" review file is filled from at most 5 spot checks per batch and from Jeff's own clicks. Empty cities are still researched from GetYourGuide's city pages. | Jeff: "go with option 1". Source: partner.getyourguide.support, API integration and requirements, read 2026-09-21. |
| 2026-09-21 | **Superseded same day by the row above.** GetYourGuide lookups were paused pending affiliate API access; Jeff answered a few minutes later that the API needs 100k monthly visits and is out of reach before launch, so the pause is replaced by mechanical search deep links (row above). Left for the record only. | Jeff: "wait for affiliate program api", superseded same session. |
| 2026-09-21 | **Day trips, two follow-ups.** (1) A day trip removed for being another Never Roam Alone city is kept as a "Nearby guides" link to that city's own guide (new `nearby` key per city, shown under the day trips; about 584 pairs). (2) Cities with no day trips get them researched from GetYourGuide's day-trip listings for that city (Jeff wrote "getmyguide.com"; getyourguide.com is meant, matching the affiliate plan), each candidate verified against the same rules; none added to fill a count. | Jeff. |
| 2026-09-21 | **Day-trip rules added to the verification pass (amends 2026-09-18).** (1) A day trip is never another city that has its own Never Roam Alone guide: 584 of 3,806 entries match a guide city by name today and 65 more contain one; each is confirmed as the same place, then removed. (2) Boat trips to another island keep the same time limits (half about 75 min, full about 3 h, each way). (3) Flights are not an option; a place reachable only by air is removed. (4) Day trips will carry GetYourGuide affiliate links, so every kept entry is looked up on getyourguide.com; one that is not listed is kept and flagged in `_needs-attention/daytrips-not-on-getyourguide.md` for Jeff's review, never removed for that alone. Batch 1 is re-checked under the new rules. | Jeff. |
| 2026-09-21 | **Map pins for cities added under "Other" on the travel map: OpenStreetMap Nominatim, "for now".** A city not in world-cities.js is looked up once per browser (at most one request per second, result or "not found" cached in localStorage for 30 days, "© OpenStreetMap contributors" shown under the map when used); only the typed city and its country are sent. No match = counted, no pin, never a guessed spot. Rejected: Google Geocoding (billed past 10,000 a month, needs a server function because no Google key may sit in page code, and the free allowance is reserved for restaurant research); Photon (already allowed, also OSM data, but run by Komoot rather than OpenStreetMap). The logic lives in `travel-stats.js`, shared by profile.html and roamer.html, and the public profile now shows the same counters and green/red pins as the owner's profile. CSP connect-src and privacy.html updated. | Jeff: "use open street map for the lookup for now" and "match the public profile page". |
| 2026-09-21 | **Footer and header: one version each, no page copies.** The footer markup lives only in `footer.js` (the homepage footer: round sand logo with glow, rust wordmark, © year, the ten links, the affiliate notice); every page has an empty `<footer data-site-footer>` and loads `footer.js` just before `nav.js`. The header is the homepage header (46px logo linking to index.html, larger "Your Travel Companion" tagline, ink bottom line). Both are styled only in `master.css`; all page-level footer, header, brand and menu CSS was deleted from the 25 pages. The plug-icon licence credit moved from the terms footer into Terms §7. 404 now loads auth.js and admin/blog-editor load i18n.js so every header has the same parts. Below 340px the language box and menu button shrink so the header fits a 320px phone. | Jeff: "the footer and navigation should be identical on all pages and there shouldn't be multiple versions"; chose the homepage header. |
| 2026-09-21 | **Countries-visited counter on the profile folds territories into their parent country** (Puerto Rico counts as the United States, Greenland as Denmark), with the same fold the homepage uses to reach 197; Western Sahara is not counted. | Jeff: "no, add to country they are a territory of". |
| 2026-09-18 | **Amends the laundromat row below: no Google API use for gyms or laundry; the Google free allowance and trial credit are reserved for the restaurant / cafe / bar research.** Laundromats come from Overture Maps (open data, storable; OpenStreetMap was tested and is thinner): category `laundromat` → "Self-service", `laundry_services` → "Drop-off & fold"; dry cleaners and hotels excluded; operating status not closed; up to 3 per neighborhood, the nearest within 1 km (no ratings, so "best match" = nearest high-confidence listing). Each has a "See it on a map" button that opens a plain Google Maps link (not an API call). Neighborhoods with no open-data listing show "Laundromat list coming soon" plus the existing "Show all laundromats in …" link. Pilot result: Lisbon and Osaka well covered; Riyadh, Essaouira and Kampala have none in Overture or OpenStreetMap. | Jeff: "gym/laundry are secondary priority… save free allowance for those tasks. If we can use sources that dont eat into that budget but still list a button for Google external map open, do so. Otherwise… coming soon". |
| 2026-09-18 | **Laundromat list per neighborhood: definition (fixed before collection).** Counts: self-service laundromats and drop-off wash-and-fold. Excluded: dry cleaners and hotel laundry. Up to 3 per neighborhood, within 1 km of the neighborhood point, "best match" first = the weighted rating used for venues (score balanced by review count) among places that are open. Names and addresses come from OpenStreetMap (storable with credit); Google is used only to rank, to confirm the place is operating, and for the exact-listing link (place ID). A laundromat Google knows but OpenStreetMap does not is shown only with its own confirming source, else left to the "Show all laundromats" link. Pilot first on Lisbon, Osaka, Riyadh, Essaouira, Kampala. | Jeff: "self service and drop off, skip dry cleaners and hotel, 3 per neighborhood is fine. the best match if that is possible". |
| 2026-09-19 | **Seasonal events join holidays in the date notice.** The itinerary's per-stop notice lists holidays (exact dates, `city-holidays.js`) and seasonal events (month-level, `city-seasons.js`). A seasonal entry that is the same occasion as a holiday in its month (Naadam, Orthodox Easter, Ramadan...) is dropped at display time and the holiday alone decides; the data keeps both, so Where to Go This Month (which does not read holidays) still lists it. The finder's 📅 pill uses the same de-duplicated list. Cards show 2 items; the rest open in a popup from "+ N more for these dates". | Jeff: "itinerary reads seasonal events, drop duplicates... we could use a modal popup to show any/all (holidays and events)". |
| 2026-09-19 | **Travel advisories Level 1-2 stay in the sheet only;** the city page keeps showing Level 3-4 only. Researched laundry price ranges are shown on the site as low–high (`laundry.usdMax`), the 23 missing emergency notes and Ulaanbaatar's seasonal events were copied from the sheet to the site. | Jeff, answering group B of `sheet-site-parity-2026-09-19.md`. |
| 2026-09-19 | **The sheet is the master and is filled at research time. Where a task changed the site without writing the sheet, the site's data fills the sheet.** Answers the open question in `sheet-site-hotel-parity.md` (2026-09-18). The full discrepancy check and the origin of each gap are in `sheet-site-parity-2026-09-19.md`: the sheet was loaded on 2026-07-26 and four bulk commits (2026-08-24 to 08-31) changed hotels and neighborhoods in `citydata/` only. | Jeff: "the sheet is supposed to contain all the material and be populated at research time. If it is not done so properly over the last tasks then we can use the site to fill the details." |
| 2026-09-18 | **Footer: one design from master.css on every page.** Per-page copies of the footer grid, link and wordmark rules are removed (9 grid, 9 link-colour, 19 wordmark). Links are one pale green (#dfeee7), line height 1.2, rows centred, wrapping on phones (600px and narrower). The link for the page on screen gets a light box (white 14%), not an underline (Jeff tried the underline and dropped it). The wordmark is the nav rust with a soft white glow, a small round sand logo on its left (`logo-round-sand.png`, cut from `media-kit/assets/logo/nra-logo-mark-on-sand-1024.png`; master copy `nra-logo-mark-round-sand-1024.png`) with a 1px sand outline, and "© year" on its right in the link green (nav.js keeps the year current). Spacing: 20px above the logo row, between it and the links, and below; the 5px top line is brand rust (was brass). The nav bar keeps its own current-section highlight. | Jeff, 2026-09-18: "the footer and nav bar should always remain uniform across all pages"; box instead of underline; rust wordmark; "something like this but white" (glow); logo for symmetry; round sand logo with a thin sand outline. |
| 2026-09-18 | **Visitor-submitted events: the public reads only the `city_events_public` view.** No submitter name or email, organizer contact only when `allow_contact` is on, published rows only. The `city_events` table is admins-only to read (`city-events-private.sql`). Same pattern as `published_photos` and `city_insights_public`: any new public read of a table holding emails goes through a view. | Jeff approved; the old policy let anyone with the public key read submitter emails. |
| 2026-09-18 | **Amends the row below: the Find modal uses a Laundry / Gyms radio toggle, not two external buttons.** Under the neighborhood dropdown, a two-part radio shows either Laundry (the city's laundry facts: price, availability, payment, from the `laundry` data) or Gyms (the researched day-pass gyms for the chosen place, or "No day-pass gyms researched in X yet"). Each view ends with a text link, "Show all laundromats in X →" / "Show all gyms in X →", that opens Google Maps. There is no researched list of individual laundromats; laundry facts are city-wide. | Jeff: "should be a radio button that shows either choice results Gym or Laundry (not an external link)… add at the bottom of the results a link that says show all laundromats in (neighborhood)". |
| 2026-09-18 | **Reverses 2026-09-17 (embedded Google map in the Find laundry & gyms modal): it is now plain links.** The modal keeps its neighborhood dropdown and the researched day-pass gyms; the map and its Laundry/Gyms tabs are replaced by two buttons, "Laundromats near …" and "Gyms near …", that open a Google Maps search in a new tab, like every other map link on the site. The public embed key is removed from city.html (Jeff to delete it in Google Cloud), the CSP goes back to `frame-src 'none'`, and privacy.html no longer mentions Google cookies. This also retires the "two keys" arrangement: only the server key in `.env` remains, and the site no longer depends on the Cloud billing account staying active. | Jeff: "switch it to plain links", after comparing embed vs link (key on the page, billing dependency, Google cookies, page weight, phones open the Maps app). |
| 2026-09-18 | **Google keys: both now in the never-roam-alone project, still two keys.** Jeff made a new server key there (Geocoding API + Places API (New), API-restricted, no website restriction) and replaced `GOOGLE_MAPS_API_KEY` in `.env`; tested OK (Places text search and a geocode). The public embed key stays separate on purpose (see the two-keys row below). The old server key in "My First Project" is to be deleted after a few days. Found the same day: the $19 billed in Sept 2026 was Geocoding beyond the free 10,000 (neighborhood points plus the landmark-coordinate passes), not a leak. Trial credit $281 left, trial ends 2026-10-30: never-roam-alone must be on "My Billing Account" and the account activated before then, or the Find laundry & gyms map may stop. | Jeff: "i've replaced the key". |
| 2026-09-18 | **Insights interviews are published only after Jeff reviews and edits them.** A shared interview is saved as pending (`city_insights`, `city-insights-setup.sql`); the Admin page's Insights tab lets him fix the name or any answer and clear answers to leave them out, then Approve & publish (the person is emailed once). There is deliberately no one-click approve in the email, so nothing skips the edit step. On the city page each column shows the newest 3 as cards (name, month shared, first 2 answers) with "Read the full interview" and "Show all". The submitter's email is never public (the page reads the `city_insights_public` view). Buttons read "Share Your Experience" (Traveler's Take) and "Share Your Insight" (Local's Perspective). | Jeff: "Edit before publishing", "Card + read full". |
| 2026-09-18 | **Amends the island row below: a guide named after an island is NOT automatically the whole island.** Test for any place outside the city itself (island or mainland): it stays a neighborhood only if (1) its lodging mostly serves tourists (a resort strip or tourist village, not mainly a place where locals live) and (2) it is one of the main places visitors to that destination actually stay. Otherwise it is a day trip. Parks, glaciers, temples and other single sites are always day trips. The small-island rule (one main town: whole island is the city) stands. Claude makes the call per place from research and records the evidence. | Jeff: "island guides with the name shouldn't automatically be treated as a whole city… Unless a place outside the city its contained in typically houses mostly tourists, it should remain a day trip". |
| 2026-09-18 | **Islands and the neighborhood rule.** On a small island (one main town, e.g. Bermuda, Anguilla) the whole island counts as the city, so its parishes, villages and beaches are valid neighborhoods and are not day trips. On a larger island with several towns (e.g. Rhodes, Djerba), each town is its own place: another town is a day trip, not a neighborhood. Reading applied until Jeff says otherwise: a guide named after the island itself (Ibiza, Phuket, Santorini) treats the whole island as the city. A separate island is always a day trip (Weizhou from Beihai). | Jeff: "for small islands the whole island counts as the city. if a larger island has multiple cities, treat them as such". |
| 2026-09-18 | **Day trips get a full verification pass before the sheet is synced.** All 3,795 entries (888 cities) are checked: real place, right country, outside the city (not a hood or landmark), reachable as a day trip (half about 75 min or less each way, full about 3 h or less), factual blurb, no duplicates. Each is kept, fixed with a source, moved half/full, or removed with a reason. Batches of 25 cities, committed per batch. Only then are the sheet's Half/Full-Day Trips columns regenerated from `day-trips.js` (1,042 of 1,786 cells disagree today, mostly empty in the sheet). Log: `_needs-attention/RESUME_daytrip-verify.md`. | Jeff: "fix the sheet day trip parity too, but only after the day trips that are on site are verified. I am certain there are mistakes on there". |
| 2026-09-18 | **Neighborhoods are never day trips, and day trips are never neighborhoods.** A neighborhood is a part of the city itself; a separate town, national park, scenic area or excursion belongs in `daytrips` only. The two lists never share an entry (same rule as landmarks vs day trips). Existing breaches are listed in `_needs-attention/hoods-that-are-day-trips.md`; the hood-picks pilot skips any hood on that list. | Jeff: "neighborhoods shouldn't be day trips and vice versa". |
| 2026-09-18 | Wrong-district neighborhood photos: a replacement counts as verified only when its GPS reverse-geocodes (OpenStreetMap) inside the hood, or its Commons category is the hood itself. A landmark whose *name* matches is not evidence (Al Andalus Mall is in Al Faiha'a; Cidade Baixa is in Porto Alegre). Where nothing verifiable exists, the photo is removed and the tile shows the labelled city stand-in with "Contribute a photo" (Jeddah/Al Andalus, Marrakech/Hivernage, Riyadh/Al Nakheel). KAFD photos are not used for Al Nakheel because Riyadh has its own KAFD hood. Porto/Baixa and Vaduz/Triesen are per Jeff's picks and are not to be re-opened. | Jeff: "use your picks, hood image placeholder for both". |
| 2026-09-18 | **Travelpayouts dropped.** Its script (emrld.ltd) is removed from the site, the CSP and the orphan story page; `consent.js` (the cookie banner, which existed only to gate that script) is deleted along with its tag on 19 pages; privacy.html no longer mentions it and its affiliate disclosure now names the eSIM links (Airalo via Impact, which stays). The monetization plan's Travelpayouts lines are struck, not deleted. Do not re-propose Travelpayouts. Same day: CSP `frame-src` set to `https://www.google.com` only, which the Find laundry & gyms map needed (it was blocked). | Jeff: "I've decided against using it". |
| 2026-09-18 | The two `NRA-MASTER.backup-*.xlsx` files in `_needs-attention/` are deleted. Git holds every version of the sheet (CLAUDE.md §7). | Jeff. |
| 2026-09-18 | Two Google Maps keys, kept separate on purpose. `GOOGLE_MAPS_EMBED_KEY` is public in city.html (the Find laundry & gyms map); it cannot be hidden and is instead restricted to websites `neverroamalone.com/*` and `*.neverroamalone.com/*` and to the Maps Embed API only (verified: loads with a neverroamalone.com referer, refused from another site). `GOOGLE_MAPS_API_KEY` (geocoding, research scripts) stays server-side in `.env` only, API-restricted, no website restriction. Billing budget alert set in Google Cloud. | Jeff set the restrictions and budget 2026-09-18. |
| 2026-09-17 | **Gym research complete: 893/893 cities, 2,656 gyms** across 801 cities; 92 cities honestly empty. Loaded into citydata and the sheet's Gyms tab over 36 batches. | Jeff's research session. |
| 2026-09-17 | **Corrects the earlier "Google Places Nearby Search" row:** the Netlify function was not shipped. What shipped is a Google Maps Embed iframe in the Find modal (Laundry/Gyms tabs, plus an "Open in Google Maps" link), using a separate browser key `GMAPS_EMBED_KEY` in city.html — the Embed API is free with no usage cap, and it is NOT the billable geocoding key in `.env`. The live OpenStreetMap list is gone; researched gyms still list above the map. | What is actually in city.html as of 2b7e2824. |
| 2026-09-17 | **Reverses 2026-09-17 (live OpenStreetMap in the Find modal):** the Find laundry & gyms modal now uses Google Places Nearby Search through the Netlify function `netlify/functions/places-nearby.js` (key server-side only, two calls per search, billed per request). Researched gyms and the neighborhood dropdown are unchanged. | Jeff, building it himself in a parallel session. |
| 2026-09-17 | The master instruction draft is approved and installed as the project CLAUDE.md (the earlier master rules kept in full as §7–8); the universal sections also live in ~/.claude/CLAUDE.md for Cowork and other projects. The draft file is retired. | Jeff: "approve the draft". |
| 2026-09-17 | Facebook and Apple sign-in buttons are on hold. The Facebook provider stays enabled in Supabase on purpose (no button on the site); do not turn it off. The Facebook button was added and removed on 2026-07-13 (`f2cb9fa2`, `bdf583b0`) with no recorded reason. When resumed, check the Facebook app's Live/Development mode, and for Apple the 6-month secret renewal and private-relay email registration for Resend. | Jeff: "wait on those". |
| 2026-09-17 | Cities with no geolocated neighborhood skip the neighborhood dropdown in the Find laundry & gyms modal and list everything city-wide with map links (laundry and gyms alike). Cities with some placed neighborhoods keep the dropdown and note the missing ones. | Jeff. |
| 2026-09-17 | Gym pilot loaded: 25 most-visited cities, 163 gyms (86 with a priced day pass, 59 free outdoor stations, 134 tied to a neighborhood), Medina empty. Milan's "Duomo / Centro" point re-placed on Piazza del Duomo after the first geocode landed in Città Studi. Continue the remaining 868 cities in batches of 25 as a scheduled task. | Pilot result. |
| 2026-09-17 | Gym research refinements: a free trial day pass counts (recorded as loc 0, shown as "free trial", never as the card's "from" price); gyms outside the five neighborhoods stay in the data and appear under "Anywhere in the city"; a dated local guide is an acceptable price source when the gym's own site publishes none (kept with its note). The Find modal carries the disclaimer "Availability and prices may not reflect the current offer — check with the business before you go." | Jeff, after the pilot's first reports. |
| 2026-09-17 | Neighborhood coordinates (`hood_geo`) geocoded with Google for 3,878 of 4,053 neighborhoods (4,535 requests, free tier). 175 unresolved are listed in `_needs-attention/hood-geo-unresolved.md` and omitted from the Find dropdown; 16 hits that were Google returning the city centre were rejected. Mirrored into the sheet as Hood N Latitude/Longitude (10 new columns). | Result of the pass Jeff approved. |
| 2026-09-17 | **Gym research definition (fixed before collection).** A gym counts if a traveler can pay for access without a membership: day, multi-day or week pass. Chains count (a country-wide chain price is used for the city's branches, marked as a chain price). Hotel gyms count only if they sell a pass to non-guests. Public sports centres count. Outdoor calisthenics parks are included as a free option. Per gym: name, address, coordinates, pass types, price per type in local currency + USD + as-of date, source URL, or "price not published" when the pass is confirmed but unpriced. Nothing estimated; aggregator-only finds are excluded. Each gym is tied to the nearest of the five neighborhoods within 1.5 km (from `hood_geo`), else "Anywhere in the city". Researched gyms list first in the Find modal, live OpenStreetMap gyms after. Stored as a `gyms` key per city and a Gyms tab in the sheet. Batches of 25, pilot first, then a scheduled task. | Jeff, 2026-09-17. |
| 2026-09-17 | Laundry and gym *locations* are a "Find laundry & gyms" modal on the hotel card: a dropdown of the city's neighborhoods plus "Anywhere in the city", then live OpenStreetMap results (nearest 8 laundries and 8 gyms within 1.5 km of the neighborhood, 3 km of the centre). Neighborhood points come from a one-time Google Geocoding pass stored as `hood_geo`; unplaced neighborhoods are omitted, never approximated. Gym *prices* remain a separate research item. | Jeff's idea, "go, use google". |
| 2026-09-16 | Laundry on the hotel card shows the attended drop-off price (per kg or lb, converted from the local currency when it is a plain number or range, shown as text otherwise) wherever there is no self-service wash+dry price. Laundry and gym *locations* are not going on the card: Jeff has a different plan for those. | Jeff: "add the drop off price. I have a different idea for gym and laundry locations". |
| 2026-09-16 | Hotel card rebuilt: "Hotel room · per night · avg" with an info bubble holding the citywide disclaimer, budget/mid/high-end on one row, then Laundry and Gym side by side. Laundry data comes from the sheet's columns via a `laundry` key in citydata (511 cities with a price, the rest show availability). Gym is a new research item and shows "coming soon" until defined and researched. | Jeff's card spec. |
| 2026-09-16 | cities.html no longer embeds a city list; it reads destinations.js (NRA_DESTINATIONS). One city list for directory and finder. add_city.py, splice.py and remove_city.py stop touching cities.html; the add-city skill's duplicate check now searches destinations.js. | Jeff: "fold it". |
| 2026-09-16 | Visitor chips on cities.html and choose.html cards carry a very small "EST." label above the figure; the per-card tilde is gone. The per-city `est` flag is now read by nothing on those pages and can be dropped from destinations.js and the schema when convenient. | Jeff. |
| 2026-09-16 | Visitor counts are estimates for all 893 cities, on purpose, and the pages say so in one on-page disclaimer. The per-city `est` flag (64 in cities.html, 864 in destinations.js) is not a research distinction and must not block folding cities.html onto destinations.js. | Jeff. |
| 2026-09-16 | cities.html's Least/Most Expensive sort uses the shared formula (mid-range per-person figure from `cost-estimator.js`), not hotel+meal+taxi. Its embedded city list now carries hotel b/h and drinks like destinations.js. The sheet's three Daily Cost columns are deleted. | Jeff. One cost ranking everywhere: directory, guide, finder. |
| 2026-09-16 | One daily-cost formula for the whole site: `cost-estimator.js` (NRA_COST). City page shows the per-person figure (two sharing a room, or one when the solo box is ticked); the finder multiplies by travelers with rooms = ceil(travelers/2), alcohol and activities on by default. Static `dailyCost` in destinations.js and the dead `daily` key in citydata are removed; the schema no longer asks for a daily figure. Taxis count per party, so per-person figures fell by up to one fare and 95 cities dropped one affordability tier. | Jeff approved the shared script, current alcohol/activities defaults on, two per room. |
| 2026-09-16 | Every city.html section is researched at add time for a new city: bars, where-to-eat, coffee/takeaway, food modal, landmark coords + photos, neighborhood photos, bikes & scooters, day trips, phrases if the language is new. Nothing deferred. | Jeff, in the master-instructions audit session. |
| 2026-09-16 | The 893 existing cities get neighborhood dining (`eat`, `cafes`) and bars backfilled in a separate upcoming task, which also fixes the schema key shapes (incl. `daily` vs `dailyCost`). | Jeff. Page reads the keys; 0/893 have eat/cafes, 40/893 have bars. |
| 2026-09-16 | No new-country research at add time: all 197 countries already have holiday, eSIM and app-link rows. Only verify presence. Exception: a newly recognised country. | Jeff. |
| 2026-09-16 | Laundry: 17 sheet columns exist, no page section. Open: build it or drop the columns. | Found in the city.html inventory audit. |
| 2026-09-16 | Destination finder: map view and Share itinerary button removed on purpose. Do not re-propose. | Jeff confirmed before commit 6e930c6f. |
| 2026-09-16 | `main` is the only long-lived branch. Task branches are fast-forwarded and deleted when done. | Jeff; five stale branches deleted. |
| 2026-09-16 | Tozeur ships with 9 landmarks; never pad to a count. | Jeff (memory: never-pad-to-hit-a-count). |
| 2026-09-15 | Food venues: replacement rule adopted (find a verifiable replacement, else delete; no dish with zero places). | Jeff, food-verification session. |
| 2026-09-02 | Canonical city count is 893; Palma, Luxembourg City, Ulan Bator, Woodlands rows deleted as duplicates/aliases. NRA-MASTER.numbers deleted; xlsx is the single master. | Jeff (memory: nra-master-xlsx-city-count). |
| 2026-07-25 | Sheet and site are always updated together (parity rule). | Jeff (CITY_SCHEMA.md). |
